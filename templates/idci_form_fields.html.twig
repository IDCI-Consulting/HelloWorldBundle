{% extends 'form_div_layout.html.twig' %}

{% block form_errors %}
    {% spaceless %}
        {% if errors|length > 0 %}
            <ul class="form-error-alert">
                {% for error in errors %}
                    <li>
                        <div data-alert class="alert-box secondary">
                            {{
                            error.messagePluralization is null
                            ? error.messageTemplate|trans(error.messageParameters, 'validators')
                            : error.messageTemplate|transchoice(error.messagePluralization, error.messageParameters, 'validators')
                            }}
                            <a class="close">&times;</a>
                        </div>
                        <script type="text/javascript">

                            $('.form-error-alert').foundation();

                            // Close alert box after 2s
                            setTimeout(function() {
                                $('.form-error-alert').fadeOut(5000, function() {
                                    $(this).remove();
                                });
                            }, 2000);
                        </script>
                    </li>
                {% endfor %}
            </ul>
        {% endif %}
    {% endspaceless %}
{% endblock form_errors %}

{% block form_widget_simple %}
    {% set type = type|default('text') %}
    <input class="input__field input__field--juro" type="{{ type }}" {{ block('widget_attributes') }}
        {% if value is not empty %}
            value="{{ value }}"
        {% endif %}
    />
{% endblock form_widget_simple %}

{% block form_row %}
    <div class="input input--juro">
        {{ form_label(form) }}
        {{ form_errors(form) }}
        {{ form_widget(form) }}
    </div>
{% endblock form_row %}

{%- block form_label -%}
    {% if label is not sameas(false) -%}
        {% if not compound -%}
            {% set label_attr = label_attr|merge({'for': id}) %}
        {%- endif %}
        {% if required -%}
            {% set label_attr = label_attr|merge({'class': (label_attr.class|default('') ~ ' required')|trim}) %}
        {%- endif %}
        {% if label is empty -%}
            {%- if label_format is not empty -%}
                {% set label = label_format|replace({
                    '%name%': name,
                    '%id%': id,
                }) %}
            {%- else -%}
                {% set label = name|humanize %}
            {%- endif -%}
        {%- endif -%}
        <label class="input__label input__label--juro" {% for attrname, attrvalue in label_attr %} {{ attrname }}="{{ attrvalue }}"{% endfor %}>
            <span class="input__label-content input__label-content--juro">
                {{ label|trans({}, translation_domain) }}
            </span>
        </label>
    {%- endif -%}
{%- endblock form_label -%}