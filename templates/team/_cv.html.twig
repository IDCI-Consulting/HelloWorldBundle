{% trans_default_domain 'cv' %}

    <section class="section-cv">
        <div class="content-container">
        <section class="columns is-multiline is-centered section-cv-header">
            <h1 class="column is-12 section-cv-title idci-title-secondary">{{ 'cv.title'|trans }}</h1>
            <p class="column is-6 section-cv-label">{{ data.identity.firstName }} {{ data.identity.lastName }} | {{ data.identity.job[app.request.locale] }}</p>
            {% if data.identity.socialNetworks is defined and data.identity.socialNetworks is not empty and data.identity.socialNetworks is not null %}
                <p class="column is-6 section-cv-links">
                    {% for socialNetwork in data.identity.socialNetworks %}
                        <a href="{{ socialNetwork.url }}"><i class="fab fa-{{ socialNetwork.name }} fa-3x"></i></a>
                    {% endfor %}
                </p>
            {% endif %}
        </section>

        <section class="section-cv-banner section-cv-banner-primary">
            <h2 class="section-cv-banner-title idci-title-secondary">{{ 'cv.about_me'|trans }}</h2>
            <table class="section-cv-banner-table">
                <tbody>
                    <tr>
                        <th scope="row">{{ 'cv.age'|trans }}</td>
                        <td>{{ get_age(data.identity.birthDate) }} {{ 'cv.years_old'|trans }}</td>
                    </tr>
                    <tr>
                        <th scope="row">{{ 'cv.email'|trans }}</th>
                        <td>{{ data.identity.email }}</td>
                    </tr>
                    {% if data.identity.phone is defined and data.identity.phone is not null and data.identity.phone is not empty %}
                        <tr>
                            <th scope="row">{{ 'cv.phone'|trans }}</th>
                            <td>{{ data.identity.phone }}</td>
                        </tr>
                    {% endif %}
                </tbody>
            </table>
        </section>

        <div class="section-cv-double">
            <section class="section-cv-double-main section-cv-banner section-cv-banner-secondary">
                <h2 class="section-cv-banner-title idci-title-secondary">{{ 'cv.professional_experiences'|trans }}</h2>
                {% for professionalExperience in data.professionalExperiences %}
                    <h3 class="section-cv-banner-subtitle">{{ professionalExperience.role[app.request.locale] }}</h3>
                    <p class="section-cv-banner-date">
                        {% if "" == professionalExperience.endDate %}
                            {{ 'cv.since'|trans }} {{ professionalExperience.startDate|format_datetime(pattern='MMMM yyyy', locale=app.request.locale) }}
                        {% else %}
                            {{ 'cv.from'|trans }} {{ professionalExperience.startDate|format_datetime(pattern='MMMM yyyy', locale=app.request.locale) }} {{ 'cv.to'|trans }} {{ professionalExperience.endDate|format_datetime(pattern='MMMM yyyy', locale=app.request.locale) }}
                        {% endif %}
                        {% if professionalExperience.company is defined and professionalExperience.company is not empty and professionalExperience.company is not null %}
                            {{ 'cv.at'|trans }} {{ professionalExperience.company }}
                        {% endif %}
                    </p>
                    {% if professionalExperience.tasks[app.request.locale] is defined and professionalExperience.tasks[app.request.locale] is not null and professionalExperience.tasks[app.request.locale] is not empty %}
                        <ul class="section-cv-banner-highlights">
                            {% for task in professionalExperience.tasks[app.request.locale] %}
                                <li class="section-cv-banner-highlights-element">{{ task|raw }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                    {% if professionalExperience.xpByCustomer is defined and professionalExperience.xpByCustomer is not null and professionalExperience is not empty %}
                        {% for experience in professionalExperience.xpByCustomer %}
                            <section class="section-cv-banner-highlights">
                                <h4 class="section-cv-banner-highlights-title">
                                    <a href="{{ experience.website }}" target="_blank">{{ experience.customer }}</a>
                                    <span>({{ experience.startDate|format_datetime(pattern='MMMM yyyy') }} - {{ experience.endDate|format_datetime(pattern='MMMM yyyy') }})</span>
                                </h4>
                                <ul class="tags section-cv-banner-highlights-tags">
                                    {% for tool in experience.tools %}
                                        <li class="tags-tag section-cv-banner-highlights-tags-tag">{{ tool }}</li>
                                    {% endfor %}
                                </ul>
                                <ul class="section-cv-banner-highlights-details">
                                    {% for task in experience.tasks[app.request.locale] %}
                                        <li class="section-cv-banner-highlights-element">{{ task|raw }}</li>
                                    {% endfor %}
                                </ul>
                            </section>
                        {% endfor %}
                    {% endif %}
                {% endfor %}
            </section>

            <section class="section-cv-double-aside section-cv-banner section-cv-banner-primary">
                <h2 class="section-cv-banner-title idci-title-secondary">{{ 'cv.skills'|trans }}</h2>
                {% for expertise in data.expertises %}
                    <table>
                        <thead><tr><th colspan="2" scope="column">{{ expertise.name[app.request.locale] }}</th></tr></thead>
                        <tbody>
                            {% for skill in expertise.skills %}
                                <tr>
                                    <th scope="row">{{ skill.name[app.request.locale] }}</th>
                                    <td>
                                        {% if skill.keywords is defined and skill.keywords is not empty and skill.keywords is not null %}
                                            {% for keyword in skill.keywords %}
                                                {{ keyword }}{% if loop.last != true %}, {% endif %}
                                            {% endfor %}
                                        {% else %}
                                            {{ skill.level[app.request.locale] }}
                                        {% endif %}
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                {% endfor %}
            </section>
        </div>

        {% if data.education is defined and data.education is not null and data.education is not empty %}
            <section class="section-cv-banner section-cv-banner-primary">
                <h2 class="section-cv-banner-title idci-title-secondary">{{ 'cv.courses'|trans }}</h2>
                {% for education in data.education %}
                    <p><span class="section-cv-banner-education-date">{{ education.endDate|date("Y") }}</span>{{ education.certificate }}</p>
                {% endfor %}
            </section>
        {% endif %}
        </div>
    </section>