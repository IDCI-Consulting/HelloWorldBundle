{% extends 'layout.html.twig' %}

{% block main_content %}
    <div class="row">
        <div class="small-12 columns">
            {% include 'partials/cv/cvRaw.html.twig' with {'cv': cv} %}
            <a class="button small export-cv" href="{{ path('cv', {'_locale': app.translator.locale, 'name': name, '_format': 'pdf'}) }}">{% trans %}Download{% endtrans %}</a>
        </div>
    </div>
{% endblock %}