<ul class="idci-inline-list">
    {% for item in menu %}
        <li>
            <a href="{{ path(item.route, {'_locale': app.translator.locale}) }}">{{ item.label|trans }}</a>
        </li>
    {% endfor %}
</ul>