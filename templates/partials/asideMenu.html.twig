{% if aside_menu is defined %}
    <ul class="show-for-large-up aside-navigation-menu">
        {% for id, title in  aside_menu %}
            <li>
                <a href="#{{ id }}" class="aside-navigation-link">
                    {{ title }}
                    <div class="idci-circle">
                        <span class="fix"></span>
                        <span class="expandable"></span>
                    </div>
                </a>
            </li>
        {% endfor %}
    </ul>
{% endif %}