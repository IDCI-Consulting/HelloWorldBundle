<div class="{{ class }} small-12 columns">
    <h3 class="capitalize sub-title">{{ title|trans }}</h3>
    <ul class="tools-list">
        {% for image in images %}
            <li class="tools-sprite {{ image }}">{{ image }}</li>
        {% endfor %}
    </ul>
</div>