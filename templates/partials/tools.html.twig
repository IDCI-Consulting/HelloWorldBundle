<em class="capitalize tool sub-title">{{ title|trans }}</em>
<ul class="tools-list {{ title }}">
    {% for techno, url in images %}
        <li>
            <a href="{{ url }}" target="_blank" class="tools-sprite {{ techno }}">{{ techno }}</a>
        </li>
    {% endfor %}
</ul>
