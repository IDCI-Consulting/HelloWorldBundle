<div class="{{ class }}">
    <h3 class="capitalize sub-title">{{ title }}</h3>
    <ul class="tools-list">
        {% for image in images %}
            <li class="tools-sprite {{ image }}">{{ image }}</li>
        {% endfor %}
    </ul>
</div>